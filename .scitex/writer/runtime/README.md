# scitex-writer runtime directory

This directory stores regenerable data for the scitex-writer package —
build registries, logs, caches, and other outputs that are re-creatable from
configuration and source code. Contents are gitignored; only this README
and `.gitkeep` are tracked.

See `01_ecosystem/06_local-state-directories.md` for the canonical layout.
